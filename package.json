{
  "name": "reits-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "echarts": "^5.6.0",
    "esbuild": "^0.28.2"
  }
}
